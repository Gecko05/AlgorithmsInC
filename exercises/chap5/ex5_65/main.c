#include <stdlib.h>
#include <stdio.h>

typedef struct node* link;
struct node{
    int item;
    link r;
    link l;
    link p;
};

int areIsomoprhic(link a, link b)
{
    link x, y;
    x = a;
    y = a;
    while (x != NULL){
        
    }
}

int main(int argc, char *argv[])
{

}