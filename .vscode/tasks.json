{
    "tasks": [
        {
            "type": "shell",
            "label": "gcc-5 build active file",
            "command": "make",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ],
    "version": "2.0.0"
}